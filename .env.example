# Add this only in Expo > Project settings > Environment variables.
# Use visibility "Sensitive" and environment "Production".
OPENAI_API_KEY=replace-with-your-openai-api-key

# Production builds already set these values in eas.json.
EXPO_UNSTABLE_DEPLOY_SERVER=1
EXPO_PUBLIC_EDIT_API_URL=/
