name: Deploy EditMyPic API

on:
  push:
    branches: ['main']

# Redeploy after configuring OPENAI_API_KEY in the production environment.
jobs:
  deploy:
    type: deploy
    name: Deploy EAS Hosting
    environment: production
    params:
      prod: true
