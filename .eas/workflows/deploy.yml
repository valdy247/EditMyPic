name: Deploy EditMyPic API

on:
  push:
    branches: ['main']

# GitHub is connected; this push triggers the first production deployment.
jobs:
  deploy:
    type: deploy
    name: Deploy EAS Hosting
    environment: production
    params:
      prod: true
